# 학생 작업물 저장·공유 구현 계획

## 1. 배경 및 목표

지금 Python·C·Pico 세 실습은 **현재 코드 한 벌**만 `localStorage` 에 자동
저장한다(`chicode.python.code`, `chicode.c.code`, `chicode.pico.code` +
`chicode.pico.circuit`). 새로고침·탭 재방문에는 견디지만:

- 다른 코드를 쓰기 시작하면 이전 코드가 **덮여서 사라진다**. "아까 만든 거"로
  못 돌아간다.
- 만든 걸 **남한테 보여줄 방법이 없다**. 교사가 "이 코드 열어보세요" 를 하려면
  코드를 화면에 띄우고 학생이 손으로 옮겨 적어야 한다. 학생이 "이거 맞아요?" 를
  물으려면 캡처를 보낸다.
- Pico 회로는 특히 아깝다 — 20분 배선한 회로가 예제 한 번 잘못 누르면 날아간다.

이 문서는 두 가지를 더한다:

- **A. 내 보관함** — 지금 코드(+회로)를 이름 붙여 여러 벌 저장하고 다시 불러온다.
- **B. 링크로 공유** — 코드(+회로)를 통째로 담은 URL 을 만든다. 그 링크를 연
  사람은 같은 코드를 에디터에서 본다.

## 2. 범위 — 의도적으로 확정한 사항

**하는 것**

- 세 실습(`/practice/python`, `/practice/c`, `/practice/pico`) 전부에 동일 패턴.
- 보관함은 **그 브라우저 안에만** 남는다(`localStorage`). 컴퓨터를 바꾸면 없다.
- 공유는 **URL 자체에 데이터를 싣는다**. 서버에 아무것도 안 올라간다.

**안 하는 것 (이번 범위 밖 — 다른 세션이 "친절하게" 넓히지 말 것)**

- 계정별/기기 간 동기화. 학생 로그인이 없다 — 이건 구조상 불가능하고,
  가능하게 하려면 Blaze 플랜이 필요하다(아래 3절).
- 교사가 제출물을 한곳에 모아 보는 화면. 그건 "학생 진행 현황 대시보드" 라는
  별개 기능이다. 여기서 만드는 공유 링크가 그 입력이 될 수는 있지만, 수집·집계는
  범위 밖이다.
- 실시간 공동 편집.
- 짧은 링크(`chico.dev/s/AB12` 같은 것). 단축에는 서버 저장소가 필요하다 —
  3절 결론과 배치된다. 링크가 길다는 건 받아들인다(6절).

## 3. 왜 Firestore 가 아니라 URL 인가 (트레이드오프)

`firestore.rules` 는 학생(비로그인) 쓰기를 **전 컬렉션에서 막고 있다.** 공유를
Firestore 로 하려면 `sharedSnippets` 같은 컬렉션에 `allow create: if true` 를
줘야 하는데:

- 인터넷의 누구나 우리 무료 플랜 할당량(1GiB / 하루 쓰기 2만 건)에 쓰레기를
  써넣을 수 있다. `exercises.ts` 머리말이 학생 풀이 기록을 Firestore 에 안 남긴
  이유와 **똑같은 판단**이다.
- 스팸·부적절 콘텐츠가 우리 DB 에 쌓이면 지울 책임도 생긴다.

URL 방식은:

- 서버 비용 0원 원칙에 그대로 맞는다.
- 학생 코드가 **어디에도 로그되지 않는다** — 해시(`#` 뒤)는 브라우저가 서버로
  보내지 않는다. 프라이버시상 더 낫다.
- 오프라인에서도 만들어진다.

단점(받아들임): 링크가 길다. 링크를 잃으면 그 버전은 사라진다(보관함이 이걸
보완한다). 아주 큰 회로는 링크가 메신저에서 잘릴 수 있다(6절에서 상한과 안내).

> 나중에 "제출 = 서버에 저장" 이 꼭 필요해지면 Blaze 플랜 + Cloud Function
> (쓰기 검증·용량 제한·TTL)이 답이다. 이번 범위 아님 — 핀잠금 문서 2절과 같은
> 자리매김이다.

## 4. 기능 A — 내 보관함

### 데이터 (localStorage)

실습별로 키 하나에 목록을 JSON 으로 둔다.

```
chicode.python.saves = [ { id, name, savedAt, code, stdin }, ... ]
chicode.c.saves      = [ { id, name, savedAt, code, stdin }, ... ]
chicode.pico.saves   = [ { id, name, savedAt, code, circuit }, ... ]
```

- `id` = `crypto.randomUUID()` (이미 `exercises.ts` 에서 쓰는 방식).
- `name` = 학생이 붙인다. 비우면 `2026-08-31 14:20` 같은 저장 시각.
- Pico 의 `circuit` 은 `CircuitSnapshot` 통째(현재 `chicode.pico.circuit` 와
  같은 모양).
- 개수 상한 **20개** — 넘으면 가장 오래된 것부터 밀어낸다(회로 캔버스
  `pushHistory` 가 50개에서 하는 것과 같은 방식). `localStorage` 는 도메인당
  5MB 라 코드 20벌은 여유롭지만, 회로까지면 커질 수 있어 저장 시 크기를
  합산해 초과하면 오래된 것부터 버리고 그래도 안 되면 "보관함이 꽉 찼어요"
  안내.

### 동작

- **저장**: 지금 에디터 내용(+stdin/회로)을 새 항목으로 추가. 같은 이름이 있으면
  "덮어쓸까요?" 확인.
- **불러오기**: 목록에서 고르면 에디터에 넣는다 — **7절의 덮어쓰기 보호를 탄다.**
- **삭제**: 항목별 휴지통.
- **이름 변경**: 인라인.

`localStorage` 이므로 실패 경로(사파리 프라이빗 모드 등 `setItem` 예외)를
try/catch 로 감싸고 "이 브라우저에서는 저장이 안 돼요" 를 조용히 띄운다 —
`CLAUDE.md` 메모리 규칙과 같은 방어.

## 5. 기능 B — 링크로 공유

### 페이로드 형식

```
버전(1자) + 인코딩(1자) + payload
```

- 버전 `1`. 형식이 바뀌면 `2` — 옛 링크를 잘못 해석하지 않게.
- 인코딩: `r` = raw(JSON 을 base64url), `z` = deflate(압축 후 base64url).
- payload 원본:
  ```
  Python/C : { "v": 1, "kind": "python", "code": "...", "stdin": "..." }
  Pico     : { "v": 1, "kind": "pico",   "code": "...", "circuit": {...} }
  ```

### 압축

`CompressionStream('deflate-raw')` — 브라우저 내장, 새 의존성 없음. 없으면
(구형 사파리) 자동으로 raw 로 떨어진다. **실측 후 결정**: raw 크기가 작으면
압축 안 하고 `r` 로 보낸다(짧은 코드는 압축이 오히려 커진다).

> 대안으로 `lz-string`(동기, ~3KB, "URL 에 싣기" 전용으로 유명)이 있다.
> `CompressionStream` 이 아이패드에서 문제되면 이걸 검토한다 — 단, 새 라이브러리
> 도입 원칙대로 실제 설치해 최소 재현으로 먼저 검증한다.

### 파싱 시 방어

- `JSON.parse` 만. `eval` 절대 없음.
- 스키마 검증: `kind` 가 이 화면과 맞는지, `code` 가 문자열인지, `circuit` 이
  `CircuitSnapshot` 모양인지. 회로는 이미 optional 필드로 앞뒤 호환을 지키는
  코드(`withDefaultBoard`, 각 필드 `?`)라 낯선 필드는 무시된다.
- 불러온 코드는 **자동 실행하지 않는다** — 에디터에 텍스트로만 들어가고, 학생이
  실행 버튼을 눌러야 돈다. (지금도 예제 불러오기가 그렇다.)
- 크기 상한(아래) 초과 링크는 "이 링크는 너무 커서 열 수 없어요" 로 거절.

### URL 설계

```
https://chico-edu.vercel.app/practice/python#s=1z<payload>
```

- **쿼리(`?s=`)가 아니라 해시(`#s=`)** 를 쓴다:
  - GitHub Pages 는 `basename=/chicode/` + 404 폴백이라 쿼리 조합이 까다롭다.
    해시는 라우터(`BrowserRouter`)가 건드리지 않아 두 배포처에서 똑같이 동작한다.
  - 해시는 서버 요청에 안 실린다 → 학생 코드가 Vercel/GitHub 액세스 로그에
    안 남는다.
- 링크를 만든 실습 경로를 그대로 쓴다(`/practice/c#s=…`). `kind` 와 경로가
  어긋나면(잘못 붙여넣음) "이 링크는 C 실습 코드예요" 안내 후 해당 경로로 이동
  제안.
- 만든 직후 `history.replaceState` 로 주소창의 `#s=…` 를 지운다 — 내가 만든
  링크를 열어둔 채로 계속 편집하면 새로고침 때 옛 코드로 되돌아가는 혼란 방지.

### 동작

- **공유 버튼** → 링크 생성 → `navigator.clipboard.writeText`(이미
  `CodeBlock.tsx`·`DebugPanel.tsx` 에서 쓰는 패턴) → "링크를 복사했어요" 토스트.
  클립보드 실패 시 링크를 `readonly input` 으로 보여주고 직접 복사하게.
- **링크 열기**: `#s=` 가 있으면 배너로 물어본다(7절).

## 6. URL 길이 상한

해시는 서버로 안 가니 브라우저 한도(수십만 자)는 사실상 문제 안 된다. 진짜
제약은 **받는 쪽 앱**(카톡·메신저가 링크를 자르거나 미리보기를 포기하는 지점)과
QR 코드다.

- 압축 후 payload **8,000자** 를 소프트 상한으로 둔다(대략 QR·메신저가 무난한
  선). 넘으면 공유 버튼이 "회로가 커서 링크로는 어려워요 — 보관함에 저장하고
  파일로 주고받으세요" 로 바뀐다.
- 실측(아래 검증)에서 전형적 회로가 이 한참 아래면 상한을 조정한다.

## 7. 불러올 때 기존 코드 보호

보관함 불러오기와 공유 링크 열기 **양쪽 공통**:

- 지금 에디터 내용이 비어있지 않고 마지막 저장/예제와 다르면, 먼저
  **자동으로 보관함에 `"불러오기 전 코드"` 로 저장**하고 나서 덮어쓴다.
- 배너/모달로 한 번 확인: "공유된 코드를 불러올까요? 지금 코드는 보관함에
  '불러오기 전 코드'로 저장됩니다. [불러오기] [취소]"
- 취소하면 아무 일도 없고 `#s=` 만 지운다.

## 8. 공통 훅으로 중복 정리

`PythonLab`·`CLab`·`PicoLab` 은 지금 거의 같은 코드를 세 벌 갖고 있다 —
`CODE_KEY` 저장 `useEffect`, `⌘/Ctrl+Enter` 핸들러, 예제 select. 저장·공유를
세 군데 복붙하면 더 심해진다. 먼저 묶는다:

```
src/practice/useDraft.ts        (신규)
  - localStorage 자동저장 (현재 코드 한 벌)
  - 보관함 목록 CRUD
  - 공유 링크 만들기 / 파싱 / 덮어쓰기 보호
  - kind('python'|'c'|'pico') 로 키·스키마 분기
```

Pico 는 회로가 있어 `circuit` 을 함께 다루도록 제네릭하게. 회로 스냅샷을
읽으려면 `CircuitCanvasHandle` 에 **`getSnapshot(): CircuitSnapshot` 를
추가**한다(내부에 `snapshotNow` 가 이미 있고, 지금은 `loadCircuit` 만
노출한다). `localStorage['chicode.pico.circuit']` 를 직접 읽는 방법도 있지만
캔버스가 소유한 키라 handle 로 가는 게 깔끔하다.

UI 컴포넌트(`SavesMenu`, `ShareButton`)도 실습 간 공유.

## 9. UI 배치

헤더의 "예제 불러오기" 셀렉트 옆에 둔다.

```
[상태뱃지]  [보관함 ▾]  [공유]  [예제 불러오기 ▾]  [▶ 실행]
```

- **보관함 ▾**: 열면 저장 목록 + 맨 위 "지금 코드 저장". 각 줄에 이름·시각·
  불러오기·삭제.
- **공유**: 누르면 링크 만들고 복사 + 토스트. (모달 없이 한 번에.)
- 좁은 화면(폰): 헤더가 이미 `flex-wrap` 이라 다음 줄로 접힌다. 아이콘+짧은
  라벨로.
- 토스트 컴포넌트가 없다 — 작게 하나 만든다(`src/components/Toast.tsx`,
  2~3초 후 사라지는 하단 알림). 이후 다른 화면에서도 쓸 수 있다.

## 10. 보안 · 프라이버시

- 공유 링크 = 코드 평문(압축은 난독화가 아니다). "링크 있는 사람은 누구나
  본다" 를 공유 버튼 툴팁에 명시.
- 학생 코드는 서버·DB 어디에도 안 간다(해시 + localStorage). 개인정보처리방침에
  한 줄 반영 검토 — "실습 코드는 기기에만 저장되며 공유 링크에는 코드가 링크
  자체에 포함됩니다".
- 파싱은 `JSON.parse` + 스키마 검증만. 자동 실행 없음.

## 11. 코드 변경 파일 (예상)

| 파일 | 변경 |
|---|---|
| `web/src/practice/useDraft.ts` (신규) | 자동저장 + 보관함 + 공유 링크 로직, kind 분기 |
| `web/src/practice/SavesMenu.tsx` (신규) | 보관함 드롭다운 |
| `web/src/practice/ShareButton.tsx` (신규) | 링크 생성·복사 |
| `web/src/practice/shareLink.ts` (신규) | 페이로드 인코딩/디코딩(버전·압축·base64url·스키마 검증) |
| `web/src/components/Toast.tsx` (신규) | 하단 토스트 |
| `web/src/pages/PythonLab.tsx` | 로컬 저장 `useEffect`·단축키를 훅으로 교체, 보관함/공유 버튼, `#s=` 처리 |
| `web/src/pages/CLab.tsx` | 위와 동일 |
| `web/src/pages/PicoLab.tsx` | 위 + 회로 포함, `#s=` 처리 |
| `web/src/pico/circuit/CircuitCanvas.tsx` | `CircuitCanvasHandle` 에 `getSnapshot()` 추가 |
| `web/src/content/PrivacyPolicy.tsx` | (검토 후) 실습 코드 저장 방식 한 줄 |

`firestore.rules` **변경 없음** — 서버를 안 쓴다.

## 12. 검증 (먼저 검증, 그다음 구현)

구현 전에 실측할 것:

1. **회로 스냅샷 크기.** 전형적 예제 회로(`pico/examples.ts`)와 "무거운"
   회로(부품 15개 + 전선 30가닥)를 JSON 직렬화해서 바이트 수 측정 →
   `deflate-raw` 압축 후 → base64url 후 최종 길이. 6절 상한이 현실적인지 확인.
2. **`CompressionStream` 아이패드 동작.** 실제 iPad Safari 에서 압축/해제 왕복.
   안 되면 `lz-string` 최소 재현 검증으로 전환 판단.
3. **두 배포처에서 링크.** Vercel(`/practice/python#s=…`)과 GitHub Pages
   (`/chicode/practice/python#s=…`) 양쪽에서 링크 열기.

구현 후:

4. `npm run dev` 로 세 실습 각각:
   - 저장 → 다른 코드 작성 → 보관함에서 원복
   - 공유 링크 생성 → 새 탭에서 열기 → 덮어쓰기 배너 → 불러오기
   - 잘못된/깨진 `#s=` → 안전하게 거절
   - kind 안 맞는 링크(파이썬 링크를 C 화면에서) → 안내
   - Pico: 회로까지 함께 오가는지, 예제 불러오기와 안 충돌하는지
5. 사파리 프라이빗 모드 → 저장 실패 안내가 뜨고 화면은 안 깨짐
6. `npm run build` → Vercel 배포, 실기기(아이패드 포함)에서 재확인
7. 커밋 전 로컬 스크린샷으로 UI 확인받기 (작업 스타일 규칙)

## 13. 나중에 (범위 밖, 참고)

- 교사용 "제출 링크 모으기" — 이 공유 링크를 붙여넣으면 목록으로 쌓이는 화면.
  이번 기능이 그 토대가 된다.
- 보관함을 파일(.json)로 내보내기/가져오기 — 기기 이동을 링크 없이.
- 연습문제(`/exercises`) 화면에도 "내 풀이 보관" — 지금은 문제별 마지막
  코드만 남는다.

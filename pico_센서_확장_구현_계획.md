# Pico 2 W 시뮬레이터 — 입출력 센서 확장 구현 계획

작성: 2026-08-25. `pico2w_시뮬레이터_구현_계획.md`(1차 구현)의 후속 문서다.
지금 부품은 5종(LED, RGB LED, 부저, 버튼, 스위치)이고 전부 `machine.Pin`(디지털
입출력)만 쓴다. 이걸 "가능한 다" 늘리는 게 목표다.

## 0. 먼저 고친 것 (선행 버그)

센서를 붙이기 전에 `machine.Pin` 자체에 버그가 있었다. `pico.worker.ts` 의
`pin_read()` 가 버튼 상태 맵(`gpioIn`)만 읽어서, **출력 핀은 `value()` 가 항상 0**
이었다. `toggle()` 이 `value(0 if value() else 1)` 이라 매번 1만 쓰게 되고,
"LED 깜빡이기" 예제가 한 번 켜진 뒤 멈춰 있었다. `gpioOut` 맵을 추가해 해결.
이 문서의 나머지 작업은 전부 이 수정 위에 올라간다.

## 1. 왜 지금 부품이 5종뿐인가

`machine` 모듈이 이 MicroPython WASM 빌드에 원래 없어서 Python 소스로 직접 만들어
`sys.modules` 에 꽂아 넣는데(1차 계획 문서 참고), 거기 `Pin` 클래스 하나만 들어있다.
ADC/PWM 이 없으니 아날로그가 필요한 부품은 "회로에 놓을 순 있는데 실제로 동작은 안
하는" 상태가 된다 — 그건 이 프로젝트가 제일 싫어하는 종류의 거짓말이라 처음부터
뺐다(`types.ts` 의 `ComponentType` 주석).

그래서 부품을 늘리는 일은 **캔버스에 글리프를 그리는 일이 아니라 런타임에 클래스를
추가하는 일**이 대부분이다. 아래 단계는 그 의존 관계 순서다.

## 2. 단계

### 1단계 — ADC (아날로그 입력)

`machine.ADC(Pin(26)).read_u16()` → 0~65535.

- 브릿지: `_chico_hw.adc_read(pin)`. 버튼(`gpioIn`)과 같은 구조로, UI 가 워커에
  `analog` 메시지를 보내 값을 갱신한다.
- **GP26/27/28 만 허용한다.** 진짜 Pico 도 ADC 는 이 세 핀뿐이다(GP29 는 내부용).
  다른 핀을 주면 한국어로 오류를 낸다 — 여기서 관대하게 굴면 학생이 실물 보드에서
  안 되는 코드를 배우게 된다.
- 부품: **가변저항**(노브를 끌어서 0~100%), **조도센서 CDS**(밝기 슬라이더).
- 실행 중(locked)에도 조작 가능해야 한다 — 버튼/스위치와 같은 이유.

### 2단계 — PWM (아날로그 출력)

`machine.PWM(Pin(15))`, `.freq()`, `.duty_u16()`, `.deinit()`.

- 브릿지: `_chico_hw.pwm_set(pin, freq, duty)`.
- LED 글리프가 duty 에 따라 **밝기**로 보이게 한다(지금은 켜짐/꺼짐 2단계).
- 부저는 duty>0 일 때 **Web Audio 로 실제 freq 소리**를 낸다. 수업 중 소리를 꺼야
  할 수 있으니 캔버스에 음소거 버튼을 같이 둔다.
- 부품: **서보모터**(duty→각도, 팔이 실제로 돌아감), **DC 모터**(속도).

### 3단계 — 디지털만 쓰는 부품 (런타임 변경 없음)

여기는 글리프 + 배선만 있으면 된다. 1·2단계와 독립이라 언제든 끼워 넣을 수 있다.

- 입력: **PIR 인체감지**, **틸트(기울기)**, **리드 스위치** — 전부 버튼과 같은
  "디지털 IN 1개" 구조라 `activeInputs` 기존 코드를 그대로 쓴다.
- 출력: **릴레이**(딸깍 + 접점 표시), **진동모터**, **신호등 LED(3색)**.
- **7세그먼트 디스플레이** — 핀 8개짜리라 배선이 많다. 캔버스에서 실제로 8줄을 긋는
  게 현실적인지 먼저 확인할 것.

### 4단계 — 별도 모듈이 필요한 것

- **DHT11/22 온습도** — `dht.DHT11(Pin(x))`, `.measure()`, `.temperature()`,
  `.humidity()`. 타이밍 프로토콜을 흉내 낼 필요 없이 Python 셰임으로 끝나서
  **의외로 싸다.** 이 단계에서 제일 먼저 할 것.
- **네오픽셀** — `neopixel.NeoPixel(Pin(x), n)`. 셰임 가능하고 그림이 화려해서
  수업 반응이 좋을 것.
- **초음파 HC-SR04** — ⚠️ 위험. 실제 MicroPython 패턴이
  `while echo.value()==0: ...` 같은 busy-wait 라, 실시간 모드(`await` 주입)와
  정면으로 부딪힌다. 워커가 멈춘 채 실제 시간만 흐르므로 `pin_read` 가 경과 µs 로
  echo 값을 계산해주면 **될 수도** 있다 — 붙이기 전에 최소 재현으로 먼저 검증할 것.
  안 되면 정직하게 빼거나, 실물과 다른 간이 API 를 쓰는 대신 그 사실을 명시한다.
- **I2C LCD 1602** — `machine.I2C` + 드라이버 라이브러리까지 필요해서 제일 무겁다.
  맨 마지막. 여기까지 오면 그때 다시 판단한다.

## 3. 공통 설계 결정

- **핀 제약은 실물 그대로.** ADC 는 GP26~28. 지키지 않으면 시뮬레이터에서만 되는
  코드를 가르치게 된다.
- **아날로그 입력값은 UI 가 워커로 밀어 넣는다.** 버튼(`{type:'button'}`)과 똑같은
  경로에 `{type:'analog'}` 를 하나 더 만든다 — 새 구조를 만들지 않는다.
- **실행 중에도 조작 가능.** 잠금(locked)의 목적은 배선 편집 금지지 상호작용 금지가
  아니다.
- **부품이 늘면 팔레트도 다시 봐야 한다.** 지금 아이콘 사이드바는 7종 기준으로 높이가
  519px(캔버스 520px)에 맞춰져 있다. 20종이 되면 스크롤이 생기거나 접이식 그룹이
  필요하다 — 3단계 들어가기 전에 결정할 것.
- **검증 방식은 그대로.** 새 클래스마다 최소 재현 코드를 실제로 브라우저에서 돌려
  값을 찍어보고 나서 부품을 붙인다. 1차 구현 때 `time.sleep` 블로킹 문제도, 이번
  `Pin.value()` 버그도 전부 그렇게 찾았다.

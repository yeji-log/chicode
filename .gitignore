.DS_Store
*.log

# 브랜드 원본 에셋 — 저장소에 올리지 않는다 (로컬에만 보관)
# 웹사이트가 표시하는 사본은 web/public/chicode.png 로 따로 있다
CHICODE_브랜드_문서.docx
/chicode.png

# 수업 노트북 원본 — 저장소에 올리지 않는다 (로컬에만 보관).
# 공개 저장소라 올리면 수업 자료가 인터넷에 공개되고, 10권 16MB 라 저장소도 무거워진다.
# 연습문제를 만들 때 진도 확인용으로 읽었고, 그 결과는 web/src/exercises/ 에 들어 있다.
# 올리기로 결정하면 이 두 줄만 지우면 된다.
파이썬(교사)/

# 에디터
.vscode/*
!.vscode/extensions.json
.idea
.vercel
.env*

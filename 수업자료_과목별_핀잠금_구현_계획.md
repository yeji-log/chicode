# 수업자료 — 과목별 탭 + 핀번호 잠금 구현 계획

## 1. 배경 및 목표

현재 `수업자료` 탭은 과목 구분 없이 모든 자료가 한 목록에 섞여 있다. 이를 과목별로 분리하고, 각 과목은 교사가 발급한 핀번호를 입력해야 들어갈 수 있도록 바꾼다.

- 과목: **정보**, **인공지능 기초** (2개로 시작)
- 각 과목은 독립된 자료 업로드/열람 공간을 가진다 (기존 수업자료 기능 그대로 재사용)
- 각 과목은 **노션 사이트 링크 1개**를 가질 수 있다 (교사가 등록/수정)
- 각 과목은 **핀번호**로 잠겨 있으며, 교사 계정에서 언제든 변경할 수 있다

## 2. 핀번호 보안 수준 (중요 — 의도적으로 확정한 사항)

**가벼운 잠금(soft lock)** 방식으로 간다. 즉:

- 핀은 "아무나 그냥 못 들어오게" 막는 용도이지 암호학적 보안 장치가 아니다.
- chicode는 Firebase **무료(Spark) 플랜**으로 운영 중이라 서버 코드(Cloud Functions)가 없다. 실제 방어선은 `firestore.rules`뿐이다.
- `materials`, `subjects` 컬렉션은 지금처럼 `read: true`를 유지한다. 즉 개발자도구로 Firestore를 직접 조회하면 핀 없이도, 심지어 핀 값 자체도 볼 수 있다. **이건 알고 받아들인 트레이드오프다.**
- 만약 나중에 "진짜" 서버 검증(핀 통과해야만 읽기 허용)이 필요해지면, Firebase Blaze 유료 플랜으로 전환 + Cloud Function 추가가 필요하다. 이번 구현 범위에는 포함하지 않는다.

## 3. 데이터 모델

### 신규: `subjects/{subjectId}` 컬렉션

```
subjects/information
subjects/ai-basics
```

각 문서 필드:

| 필드 | 설명 |
|---|---|
| `name` | 표시 이름 ("정보", "인공지능 기초") |
| `pin` | 핀번호 (문자열, 4자리 권장) |
| `notionUrl` | 노션 사이트 링크 (선택, 과목당 1개) |
| `order` | 탭 정렬 순서 |

### 기존 `materials/{id}` 문서

- `subjectId` 필드 추가 → 어느 과목 소속인지 표시
- 기존에 이미 올라간 자료는 `subjectId`가 없으므로 별도 마이그레이션 필요 (7번 참고)

## 4. `firestore.rules` 변경

```
subjects/{subjectId}
  allow read: if true;                 // 과목 목록·핀 확인용
  allow create, update, delete: if isTeacher();
```

`materials`, `materials/{id}/chunks/{n}` 규칙은 기존과 동일하게 유지 (`read: true`, 쓰기는 `isTeacher()`).

## 5. 학생 화면 흐름 (`/materials`)

1. `/materials` 접속 → 과목 카드 2개(정보 / 인공지능 기초) 노출 (Firestore `subjects`에서 로드)
2. 과목 카드 클릭 → 핀번호 입력 모달
3. 핀 일치 → `sessionStorage`에 `unlocked:{subjectId}=true` 기록 → `/materials/:subjectId`로 이동
   - `sessionStorage` 사용 이유: 브라우저를 닫으면 초기화되어, 공용 PC에서 다음 학생이 다시 입력하게 됨
4. `/materials/:subjectId`: 기존 자료 목록/뷰어 UI를 그대로 재사용하되 해당 과목 자료만 필터링, 상단에 노션 링크 버튼 노출
5. 핀 오답 시 에러 메시지만 표시. 시도 횟수 제한 등은 넣지 않는다 (가벼운 잠금 취지에 맞지 않음)

## 6. 교사 화면 흐름 (`/teacher`)

- 로그인된 교사는 핀 없이 모든 과목에 접근
- 대시보드 상단에 과목 탭(정보 / 인공지능 기초) 추가
- 각 과목 섹션:
  - 기존 업로드 폼 재사용 (과목 컨텍스트 주입, 해당 과목으로 저장)
  - 자료 목록 (과목별 필터)
  - **과목 설정 카드**: 노션 링크 입력/수정, 핀번호 입력/수정 (마스킹 없이 그대로 노출 — 가벼운 잠금이므로 문제 없음)

## 7. 라우팅 변경 (`App.tsx`)

- `/materials` → 과목 선택 화면
- `/materials/:subjectId` → 핀 게이트 → 자료 화면
- 상단 네비 "수업자료" 탭 링크는 `/materials` 그대로 유지

## 8. 코드 변경 파일 목록 (예상)

| 파일 | 변경 |
|---|---|
| `web/src/lib/subjects.ts` (신규) | `listSubjects`, `getSubject`, `updateSubject(name/pin/notionUrl)` |
| `web/src/lib/materials.ts` | `MaterialMeta.subjectId` 추가, `listMaterials(subjectId)`로 필터 지원 |
| `web/src/pages/Materials.tsx` | 과목 선택 + 핀 모달 + 잠금 해제 상태 관리로 리팩터링 (필요 시 하위 컴포넌트 분리) |
| `web/src/pages/Teacher.tsx` | 과목 탭, 과목별 업로드/목록, 노션 링크·핀 수정 UI 추가 |
| `web/src/App.tsx` | `/materials/:subjectId` 라우트 추가 |
| `firestore.rules` | `subjects` 규칙 추가 |

## 9. 마이그레이션

- 기존에 이미 올라간 자료가 있다면 Firebase 콘솔에서 수동으로 `subjectId` 지정 (자료 개수가 적으므로 수동이 가장 빠름)
- `subjects/information`, `subjects/ai-basics` 문서는 Firebase 콘솔에서 최초 1회 직접 생성 (이름 + 초기 핀 세팅), 이후에는 교사 대시보드에서 수정

## 10. 검증

1. `npm run dev`로 로컬 확인
   - 학생 플로우: 핀 성공/실패, 과목별 자료 분리, 노션 링크 버튼
   - 교사 플로우: 과목별 업로드/삭제, 노션 링크·핀 수정
2. `firestore.rules` 배포 후 콘솔에서 규칙 재확인
3. `npm run build` → Vercel 배포
